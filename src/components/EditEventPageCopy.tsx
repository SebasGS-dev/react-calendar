import { useParams } from "react-router-dom";

const EditEventPageCopy = () => {

    const params = useParams();
    console.log(params)

    return (
        <h1> HOLA </h1>
    );
};

export default EditEventPageCopy;
